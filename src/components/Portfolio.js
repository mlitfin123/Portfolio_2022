import React from 'react';
import ProjectList from './ProjectList';

const Portfolio = () => {
    return (
        <main>
            <ProjectList/>
        </main>
    );
}

export default Portfolio;